[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "permisapi-mcp"
version = "0.5.3"
description = "Serveur MCP pour PermisAPI : 13 outils pour consulter 1,2 M+ permis de construire de France (2014-2026) depuis Claude Desktop, Cursor, Windsurf et tout client MCP-compatible. v0.5.3 ajoute le tool search_permits_in_polygon (recherche permis dans un polygone GeoJSON custom, killer feature foncière ZAC plan Business+) pour le sprint 10 cadastre Prio 5."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Evan Caroux", email = "evan@permisapi.fr" },
]
keywords = ["permis", "construire", "france", "mcp", "claude", "chatgpt", "anthropic", "real-estate", "sitadel"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Natural Language :: French",
]
dependencies = [
    "mcp>=1.0.0",
    "permisapi-client>=0.7.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
]
sse = [
    "starlette>=0.40",
    "uvicorn[standard]>=0.30",
]

[project.scripts]
permisapi-mcp = "permisapi_mcp.server:main"
permisapi-mcp-sse = "permisapi_mcp.sse_server:main"

[project.urls]
Homepage = "https://permisapi.fr/mcp"
Documentation = "https://permisapi.fr/mcp"
Setup = "https://github.com/Evan-Crx/permisapi-mcp/blob/main/README.md"
Source = "https://github.com/Evan-Crx/permisapi-mcp"
Issues = "https://github.com/Evan-Crx/permisapi-mcp/issues"

[tool.hatch.build.targets.wheel]
packages = ["permisapi_mcp"]
